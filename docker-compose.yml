version: '3.8'

services:
  backend:
    build: ./backend
    container_name: moyun-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./data/uploads:/app/uploads
      - ./data/db:/app/data
    environment:
      - DMX_API_KEY=${DMX_API_KEY}
      - DMX_BASE_URL=${DMX_BASE_URL:-https://www.dmxapi.cn/v1}
      - DMX_MODEL=${DMX_MODEL:-gpt-5-mini}
      - VISION_MODEL=${VISION_MODEL:-Doubao-1.5-vision-pro-32k}
      - PORT=3001
<<<<<<< Updated upstream
      - DB_PATH=/app/data/moyun.db
=======
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3001/api/history?page=1&pageSize=1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
>>>>>>> Stashed changes

  frontend:
    build: ./frontend
    container_name: moyun-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
